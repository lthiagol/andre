FROM rust:1.77-slim

RUN apt-get update && apt-get install -y --no-install-recommends \\
    git \\
    make \\
    stow \\
    curl \\
    gnupg \\
    perl \\
    coreutils \\
    && rm -rf /var/lib/apt/lists/*

WORKDIR /workspace

ENTRYPOINT [\"\u0022/bin/bash\"\u0022]
